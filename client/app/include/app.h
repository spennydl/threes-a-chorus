/**
 * @file app.h
 * @brief Application module. Starts and runs the application.
 * @author Jet Simon
 */
#pragma once

/**
 * Start the app using the main thread of the program (blocking)
 */
void
App_runApp(char* serverIp);
